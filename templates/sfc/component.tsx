import React from 'react';
import { %component%Props } from './props';
import './%component%.scss';

export const %component%: React.SFC<%component%Props> = (props) => {
    const { } = props;

    return (
        <div className="%className% alt_%className%">

        </div>
    );
};